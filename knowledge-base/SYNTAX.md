# Markdown — Полная шпаргалка по синтаксису

---

## 📌 Оглавление
1. [Заголовки](#заголовки)
2. [Форматирование текста](#форматирование-текста)
3. [Списки](#списки)
4. [Код](#код)
5. [Ссылки и изображения](#ссылки-и-изображения)
6. [Цитаты](#цитаты)
7. [Таблицы](#таблицы)
8. [Горизонтальный разделитель](#горизонтальный-разделитель)
9. [Экранирование символов](#экранирование-символов)
10. [HTML-теги в Markdown](#html-теги-в-markdown)

---

## Заголовки

Заголовки создаются с помощью символа `#`. Количество `#` определяет уровень:

```markdown
# Заголовок 1 уровня (H1)
## Заголовок 2 уровня (H2)
### Заголовок 3 уровня (H3)
#### Заголовок 4 уровня (H4)
##### Заголовок 5 уровня (H5)
###### Заголовок 6 уровня (H6)
```

> ⚠️ После `#` обязателен пробел перед текстом.

---

## Форматирование текста

### Жирный

```markdown
**Жирный текст**
__Тоже жирный__
```

**Жирный текст**

### Курсив

```markdown
*Курсивный текст*
_Тоже курсив_
```

*Курсивный текст*

### Жирный + Курсив

```markdown
***Жирный и курсив***
```

***Жирный и курсив***

### Зачёркнутый

```markdown
~~Зачёркнутый текст~~
```

~~Зачёркнутый текст~~

### Подчёркнутый (HTML)

```markdown
<u>Подчёркнутый текст</u>
```

<u>Подчёркнутый текст</u>

### Верхний и нижний индекс (расширенный MD)

```markdown
x^2^    → x в степени 2
H~2~O   → H₂O
```

---

## Списки

### Ненумерованный список

```markdown
- Пункт 1
- Пункт 2
- Пункт 3
```

- Пункт 1
- Пункт 2
- Пункт 3

Вместо `-` можно использовать `*` или `+`.

### Нумерованный список

```markdown
1. Первый
2. Второй
3. Третий
```

1. Первый
2. Второй
3. Третий

### Вложенные списки

```markdown
- Родитель
  - Дочерний пункт
    - Вложенный глубже
- Другой родитель
```

- Родитель
  - Дочерний пункт
    - Вложенный глубже
- Другой родитель

### Список задач (GitHub Flavored Markdown)

```markdown
- [x] Выполнено
- [ ] Не выполнено
- [x] Тоже выполнено
```

- [x] Выполнено
- [ ] Не выполнено
- [x] Тоже выполнено

---

## Код

### Инлайн-код

Оберни фрагмент в одиночные обратные кавычки:

```markdown
Используй функцию `print()` для вывода.
```

Используй функцию `print()` для вывода.

### Блок кода (с подсветкой синтаксиса)

Оберни код тройными обратными кавычками и укажи язык:

````markdown
```python
x = 10

def greet(name: str) -> str:
    return f"Hello, {name}!"

print(greet("World"))
```
````

```python
x = 10

def greet(name: str) -> str:
    return f"Hello, {name}!"

print(greet("World"))
```

### Популярные языки для подсветки

| Язык       | Идентификатор |
|------------|---------------|
| Python     | `python`      |
| JavaScript | `javascript`  |
| TypeScript | `typescript`  |
| Bash       | `bash`        |
| SQL        | `sql`         |
| JSON       | `json`        |
| YAML       | `yaml`        |
| HTML       | `html`        |
| CSS        | `css`         |
| Dockerfile | `dockerfile`  |

---

## Ссылки и изображения

### Ссылка

```markdown
[Текст ссылки](https://example.com)
[Ссылка с подсказкой](https://example.com "Подсказка при наведении")
```

[Текст ссылки](https://example.com)

### Ссылка-якорь (внутри документа)

```markdown
[Перейти к заголовку](#заголовки)
```

### Автоссылка

```markdown
<https://example.com>
<example@email.com>
```

### Изображение

```markdown
![Альтернативный текст](путь/к/картинке.png)
![Картинка из интернета](https://example.com/image.png "Подсказка")
```

### Изображение со ссылкой

```markdown
[![Альт текст](image.png)](https://example.com)
```

---

## Цитаты

```markdown
> Это обычная цитата.

> Многострочная цитата.
> Продолжение на следующей строке.

>> Вложенная цитата.
```

> Это обычная цитата.

> Многострочная цитата.
> Продолжение на следующей строке.

>> Вложенная цитата.

---

## Таблицы

```markdown
| Столбец 1 | Столбец 2 | Столбец 3 |
|-----------|:---------:|----------:|
| Влево     |  Центр    |    Вправо |
| Данные    |  Данные   |    Данные |
```

| Столбец 1 | Столбец 2 | Столбец 3 |
|-----------|:---------:|----------:|
| Влево     |  Центр    |    Вправо |
| Данные    |  Данные   |    Данные |

**Выравнивание:**
- `:---` — по левому краю (по умолчанию)
- `:---:` — по центру
- `---:` — по правому краю

---

## Горизонтальный разделитель

```markdown
---
***
___
```

Все три варианта дают одинаковый результат:

---

## Экранирование символов

Если нужно вывести спецсимвол буквально, поставь перед ним `\`:

```markdown
\*  не курсив\*
\#  не заголовок
\`  не код
\[  не ссылка
```

Полный список экранируемых символов:

```
\ \` \* \_ \{ \} \[ \] \( \) \# \+ \- \. \! \|
```

---

## HTML-теги в Markdown

Markdown поддерживает встроенный HTML:

```markdown
<b>Жирный через HTML</b>
<i>Курсив через HTML</i>
<u>Подчёркнутый</u>
<s>Зачёркнутый</s>
<kbd>Ctrl</kbd> + <kbd>C</kbd>
<br> — перенос строки
<details>
  <summary>Раскрываемый блок</summary>
  Скрытый контент здесь
</details>
```

### Пример раскрываемого блока:

<details>
  <summary>Нажми чтобы раскрыть</summary>
  Вот скрытый контент!
</details>

---

## 🔍 Совместимость

| Возможность              | Basic MD | GitHub (GFM) | Obsidian | Notion |
|--------------------------|:--------:|:------------:|:--------:|:------:|
| Заголовки                |    ✅    |      ✅      |    ✅    |   ✅   |
| Жирный / Курсив          |    ✅    |      ✅      |    ✅    |   ✅   |
| Таблицы                  |    ❌    |      ✅      |    ✅    |   ✅   |
| Зачёркивание `~~`        |    ❌    |      ✅      |    ✅    |   ✅   |
| Список задач `- [x]`     |    ❌    |      ✅      |    ✅    |   ✅   |
| Подсветка кода           |    ❌    |      ✅      |    ✅    |   ✅   |
| `<details>` / `<kbd>`    |    ❌    |      ✅      |    ⚠️    |   ❌   |

---

*Создано как шпаргалка по Markdown-синтаксису*
