# 📚 DotKnow — Knowledge Base

Персональный репозиторий знаний для повторения изученных тем.

## Структура

```
knowledge-base/
├── _meta/
│   └── topics_index.json   # Индекс всех тем (используется ботом)
├── python/
│   ├── OVERVIEW.md         # Навигация по Python-разделу
│   ├── basics/
│   ├── oop/
│   ├── async/
│   └── decorators/
└── (в будущем) javascript/, sql/, algorithms/
```

## Разделы

| Раздел | Описание |
|--------|----------|
| [Python](./python/OVERVIEW.md) | Темы по Python |

## Как добавить новую тему

```bash
python scripts/new_topic.py --lang python --section basics --topic my_topic
```

## Уровни сложности

| Уровень | Описание |
|---------|----------|
| `easy`   | Базовые концепции |
| `medium` | Стандартный уровень (по умолчанию) |
| `hard`   | Продвинутые темы |

## Типы заданий для бота

| Тип | Описание |
|-----|----------|
| `theory`       | Вопросы по теории |
| `code_writing` | Написать код с нуля |
| `find_the_bug` | Найти ошибку в коде |
| `fill_the_gap` | Заполнить пропуск в коде |


Как добавлять новые темы
Теперь не нужно создавать файлы вручную — один скрипт делает всё:

# Создать тему "closures" в python/basics
`python scripts/new_topic.py --lang python --section basics --topic closures`

# Создать тему в другом разделе
`python scripts/new_topic.py --lang python --section oop --topic inheritance`

# В будущем — другой язык
`python scripts/new_topic.py --lang javascript --section basics --topic variables`
